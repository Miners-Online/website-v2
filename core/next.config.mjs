/** @type {import('next').NextConfig} */
const nextConfig = {
    // experimental: {
    //     esmExternals: 'loose',
    // },
    transpilePackages: ['react-markdown-preview']
};

export default nextConfig;
